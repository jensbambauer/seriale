backend:
  name: git-gateway
  branch: master

# Lokales Testen ohne Netlify
local_backend: true

media_folder: "site/static/img"
public_folder: "/img"

# Collections are defined in src/js/cms.jsx (CMS_MANUAL_INIT = true)
# Do NOT add collections here - they will cause duplicate name errors
collections: []
